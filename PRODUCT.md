# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

초등학교 3~6학년 학생이 체육 수업에서 교사와 함께 사용합니다. 학생은 짧은 활동 시간 동안 고정된 가상 경기판을 보고, 공을 가진 선수·수비·열린 패스 길·다음 지원 위치를 차례로 판단합니다.

## Product Purpose

`빈 공간 패스 전술판`은 실제 경기 결과나 운동 능력을 평가하지 않고, 7×5 가상 경기판에서 수비가 막지 않은 패스 길과 패스 뒤 지원 위치를 공간 근거로 설명하도록 돕는 정적 학습 앱입니다. 성공은 점수나 순위가 아니라 학생이 관찰 → 예측 → 이동·패스 → 수비 변화 → 지원의 흐름을 따라 근거를 남기는 것입니다.

## Positioning

이 앱은 런타임 무작위 생성이나 숨은 물리 공식 대신, 교과·문구·판정이 검수 대상으로 분리된 여섯 개 고정 미션과 명시적인 공간 근거를 사용합니다. 열린 길이 둘 이상이면 한 답을 억지로 정답 처리하지 않고 검수된 복수 해법을 인정합니다.

## Operating Context

- 권장 활동 시간은 15~25분입니다.
- 학생은 마우스·터치·키보드로 선수, 라디오 선택지, 지원 칸을 조작합니다. 드래그는 필수가 아닙니다.
- 앱은 입구 → 경기판 관찰 → 패스 전 예측 → 받을 사람 이동 → 패스 길 확인 → 수비 공개 → 다음 지원 → 전술 기록의 흐름을 제공합니다.
- 학생 응답은 현재 탭 메모리에만 있으며 새로고침하면 사라집니다. 기록을 남기려면 결과 화면에서 인쇄합니다.

## Capabilities and Constraints

- Vite + React + TypeScript 정적 SPA이며 현재 GitHub Pages 하위 경로 베이스를 유지합니다.
- 정확히 6개 미션을 제공하고 미션·상태·패스 길·전이 ID는 기존 문자열 계약을 유지합니다.
- `src/domain/passEvaluator.ts`만 판정 경계이며, UI는 콘텐츠 정답 배열을 직접 판정하지 않습니다.
- 서버, 로그인, 분석, 광고, 쿠키, `localStorage`, `sessionStorage`, IndexedDB, 외부 네트워크 요청, 런타임 생성형 AI를 사용하지 않습니다.
- 학생 이름·학급·번호·실제 사진·식별자 입력이나 저장을 추가하지 않습니다.
- 학생 대상 TTS, 음성 재생·녹음·내레이션과 VoiceOver 구현·검증은 범위에서 제외합니다. 키보드·의미 있는 HTML·포커스·자동 접근성 검사는 유지합니다.
- 앱은 밝은 교실용 라이트 모드로 유지하며 `prefers-color-scheme: dark` 기반 다크 모드는 추가하지 않습니다.
- 관련 TS·TSX·CSS 파일은 500줄 미만이어야 합니다.

## Brand Commitments

- 제품명은 `빈 공간 패스 전술판`으로 유지합니다.
- 어린이에게 직접 말하는 짧고 구체적인 한국어를 사용합니다.
- 실제 경기 전체를 보장하거나 학생을 평가한다는 표현을 사용하지 않습니다.
- 화면에 작은 `업데이트 내역` 버튼을 유지하고 실제 개발·개선 날짜와 짧은 내용을 기록합니다.

## Evidence on Hand

- 검수 대상 콘텐츠: `src/content/missions/`, `src/content/validateContent.ts`, `docs/content-review.md`
- 판정·상태 근거: `src/domain/`, `src/app/sessionReducer.ts`, 기존 컴포넌트 테스트와 E2E
- 기존 일반 장식 자산: `src/assets/generated/bright-gym-tactics-board.webp`
- 자산 권리 기록: `docs/image-rights-ledger.md`
- 사람이 별도로 확인할 교과 내용·어린이용 문구·출시 승인은 자동화 증거와 분리합니다.

## Product Principles

1. 학생은 한 화면에서 지금 할 일을 하나만 분명하게 알아야 합니다.
2. 공간 근거를 선택 과정과 피드백에 함께 남겨 회상을 줄이고 이해를 돕습니다.
3. 정답보다 관찰 → 계획 → 변화 대응 → 지원의 사고 순서를 보여 줍니다.
4. 모형의 한계를 숨기지 않고, 실제 경기 능력에 대한 과장된 결론을 만들지 않습니다.
5. 장식은 경기판과 학습 흐름을 돕는 범위에서만 사용합니다.

## Accessibility & Inclusion

- 기본 본문은 16px 이상, 줄 간격은 1.6 이상, 주요 터치 목표는 44px 이상을 유지합니다.
- `:focus-visible`, 논리적인 Tab 순서, Enter/Space 조작, 단계 전환 시 제목 포커스와 스크롤을 유지합니다.
- 상태는 색상만으로 전달하지 않고 텍스트·형태·선택 상태를 함께 제공합니다.
- `prefers-reduced-motion: reduce`와 앱의 `모션 줄이기` 토글에서 `gi-pulse`를 정적 강조로 낮춥니다.
- 320/375/768/1280px 및 200% 글자 확대에서 가로 넘침 없이 핵심 행동을 수행할 수 있어야 합니다.
